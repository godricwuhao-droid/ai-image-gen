# AI图像生成网站 - 下一阶段开发计划

## 项目当前状态

**V2.0 完成时间**: 2026-04-25

### ✅ 已完成功能

| 模块 | 功能 | 完成度 |
|------|------|--------|
| 用户系统 | 注册/登录/登出 | 100% |
| 图片生成 | 生成功能（含OpenAI调用） | 100% |
| UI设计 | 现代化界面 | 100% |
| 艺术风格 | 12种预设风格 | 100% |
| 尺寸选择 | 默认+自定义 | 100% |
| 画廊页面 | 前端UI+Mock数据 | 100% |
| 模板页面 | 前端UI+Mock数据 | 100% |
| 定价页面 | 套餐展示 | 100% |
| 历史记录 | 前端UI | 100% |
| 收藏页面 | 前端UI+Mock数据 | 100% |
| 个人中心 | 前端UI | 100% |
| 管理后台 | 仪表盘/用户/订单 | 100% |

### ⚠️ 前端完成但后端待对接

| 模块 | 前端状态 | 后端状态 | 说明 |
|------|----------|----------|------|
| 画廊 | ✅ Mock数据 | ❌ 待开发 | 需API支持真实数据 |
| 收藏 | ✅ Mock数据 | ❌ 待开发 | 需API持久化 |
| 模板 | ✅ Mock数据 | ❌ 待开发 | 需API保存功能 |
| 支付 | ✅ 模拟购买 | ❌ 待开发 | 需Stripe集成 |
| 积分 | ✅ 显示余额 | ❌ 待开发 | 需充值/消耗逻辑 |

---

## 下一阶段目标：V3.0 完整数据闭环

**核心目标**: 将前端Mock数据替换为真实后端数据，实现完整的数据流

**预计周期**: 2周（40小时）

---

## 一、P0 必须完成

### 1. 真实画廊数据系统 ⏱️ 6小时

#### 1.1 数据库模型

```sql
-- 修改generations表添加公开字段
ALTER TABLE generations 
ADD COLUMN is_public BOOLEAN DEFAULT FALSE,
ADD COLUMN likes_count INT DEFAULT 0,
ADD COLUMN views_count INT DEFAULT 0;
```

#### 1.2 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/galleries` | GET | 获取公开画廊列表（分页/筛选） |
| `/api/v1/galleries/popular` | GET | 热门图片（点赞数排序） |
| `/api/v1/galleries/{id}` | GET | 图片详情（增加浏览数） |
| `/api/v1/galleries/{id}/like` | POST | 点赞 |
| `/api/v1/galleries/{id}/like` | DELETE | 取消点赞 |
| `/api/v1/generations/{id}/publish` | POST | 将生成图片设为公开 |

#### 1.3 前端修改

- `GalleryPage.tsx`: 从API获取真实数据
- `HistoryPage.tsx`: 添加"公开到画廊"按钮
- 图片详情弹窗：显示真实点赞数、浏览数

#### 1.4 测试用例

- [ ] 未登录用户查看画廊
- [ ] 登录用户点赞/取消点赞
- [ ] 查看点赞后的计数更新
- [ ] 生成图片后公开到画廊

---

### 2. 收藏功能持久化 ⏱️ 4小时

#### 2.1 数据库模型

```sql
CREATE TABLE favorites (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    generation_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (generation_id) REFERENCES generations(id),
    UNIQUE KEY unique_favorite (user_id, generation_id)
);

CREATE TABLE likes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    generation_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (generation_id) REFERENCES generations(id),
    UNIQUE KEY unique_like (user_id, generation_id)
);
```

#### 2.2 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/favorites` | GET | 我的收藏列表 |
| `/api/v1/favorites` | POST | 添加收藏 |
| `/api/v1/favorites/{id}` | DELETE | 删除收藏 |
| `/api/v1/likes` | GET | 我点赞的列表 |
| `/api/v1/likes` | POST | 点赞（画廊） |
| `/api/v1/likes/{id}` | DELETE | 取消点赞 |

#### 2.3 前端修改

- `FavoritesPage.tsx`: 从API获取真实数据
- `GalleryPage.tsx`: 点赞调用真实API
- `HistoryPage.tsx`: 收藏按钮调用API

---

### 3. 模板保存功能 ⏱️ 4小时

#### 3.1 数据库模型

```sql
CREATE TABLE templates (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(100) NOT NULL,
    prompt TEXT NOT NULL,
    category VARCHAR(50),
    is_public BOOLEAN DEFAULT FALSE,
    usage_count INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 3.2 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/templates` | GET | 预设模板列表 |
| `/api/v1/my-templates` | GET | 我的模板列表 |
| `/api/v1/my-templates` | POST | 保存新模板 |
| `/api/v1/my-templates/{id}` | PUT | 更新模板 |
| `/api/v1/my-templates/{id}` | DELETE | 删除模板 |

#### 3.3 前端修改

- `TemplatesPage.tsx`: 区分预设和我的模板
- 添加"保存到我的模板"按钮
- 添加"使用"按钮到保存的模板

---

## 二、P1 重要功能

### 4. 积分系统完善 ⏱️ 4小时

#### 4.1 数据库模型

```sql
ALTER TABLE users ADD COLUMN credits INT DEFAULT 0;

CREATE TABLE credit_transactions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    amount INT NOT NULL,  -- 正数充值，负数消耗
    type VARCHAR(50) NOT NULL,  -- purchase/generation/refund
    description VARCHAR(255),
    balance_before INT,
    balance_after INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 4.2 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/credits/balance` | GET | 获取积分余额 |
| `/api/v1/credits/transactions` | GET | 交易历史 |
| `/api/v1/credits/deduct` | POST | 消耗积分（生成图片时） |
| `/api/v1/credits/add` | POST | 充值积分（管理员） |

#### 4.3 前端修改

- `ProfilePage.tsx`: 显示积分余额和交易记录
- `HomePage.tsx`: 显示用户积分余额
- 生成图片前检查积分是否足够

---

### 5. 支付系统集成 ⏱️ 6小时

#### 5.1 数据库模型

```sql
CREATE TABLE packages (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    credits INT NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    package_id BIGINT,
    amount DECIMAL(10, 2) NOT NULL,
    credits INT NOT NULL,
    payment_method VARCHAR(50),
    payment_status VARCHAR(50) DEFAULT 'pending',
    transaction_id VARCHAR(255),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (package_id) REFERENCES packages(id)
);
```

#### 5.2 Stripe集成

```python
# 后端Stripe支付
import stripe

stripe.api_key = settings.STRIPE_SECRET_KEY

@router.post("/create-payment-intent")
async def create_payment_intent(amount: int, currency: str = "usd"):
    intent = stripe.PaymentIntent.create(
        amount=amount * 100,  # Stripe使用分
        currency=currency,
    )
    return {"client_secret": intent.client_secret}
```

#### 5.3 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/packages` | GET | 套餐列表 |
| `/api/v1/orders` | GET | 订单历史 |
| `/api/v1/orders` | POST | 创建订单 |
| `/api/v1/orders/{id}/pay` | POST | Stripe支付 |
| `/api/v1/orders/{id}/webhook` | POST | Stripe回调 |

#### 5.4 前端修改

- `PricingPage.tsx`: 添加Stripe支付组件
- `ProfilePage.tsx`: 添加订单历史和充值入口

---

### 6. 通知系统 ⏱️ 3小时

#### 6.1 数据库模型

```sql
CREATE TABLE notifications (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type VARCHAR(50) NOT NULL,
    title VARCHAR(255) NOT NULL,
    content TEXT,
    data JSON,  -- 附加数据
    is_read BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 6.2 后端API

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/notifications` | GET | 通知列表 |
| `/api/v1/notifications/unread` | GET | 未读数量 |
| `/api/v1/notifications/{id}/read` | PUT | 标记已读 |

#### 6.3 前端修改

- 添加通知图标到Header
- 显示未读数量badge
- 通知下拉列表

---

## 三、P2 增强功能

### 7. 用户体验优化 ⏱️ 4小时

- [ ] 图片加载loading动画
- [ ] 生成进度实时显示
- [ ] 错误重试机制
- [ ] 离线提示

### 8. 性能优化 ⏱️ 3小时

- [ ] 图片懒加载
- [ ] API响应缓存
- [ ] 数据库索引优化

### 9. 管理功能增强 ⏱️ 4小时

- [ ] 图片审核功能
- [ ] 用户举报处理
- [ ] 数据统计报表

---

## 四、技术任务

### 代码规范化

```bash
# 添加ESLint配置
npm run lint

# 添加单元测试
npm test

# 后端测试
pytest tests/ -v --cov
```

### 文档完善

- [ ] API接口文档 (Swagger)
- [ ] 数据库ER图
- [ ] 部署文档

---

## 五、里程碑

### Week 1: 数据闭环

| 日期 | 任务 | 负责人 |
|------|------|--------|
| Day 1-2 | 画廊后端API开发 | - |
| Day 2-3 | 收藏后端API开发 | - |
| Day 3-4 | 模板后端API开发 | - |
| Day 5 | 前端对接真实数据 | - |
| Day 5-7 | 测试和修复 | - |

### Week 2: 商业化

| 日期 | 任务 | 负责人 |
|------|------|--------|
| Day 8-9 | 积分系统开发 | - |
| Day 10-11 | Stripe支付集成 | - |
| Day 12-13 | 通知系统开发 | - |
| Day 14 | 测试和部署 | - |

---

## 六、预计工作量

| 阶段 | 功能 | 工时 | 优先级 |
|------|------|------|--------|
| P0 | 真实画廊数据系统 | 6h | 🔴 |
| P0 | 收藏功能持久化 | 4h | 🔴 |
| P0 | 模板保存功能 | 4h | 🔴 |
| P1 | 积分系统完善 | 4h | 🟡 |
| P1 | 支付系统集成 | 6h | 🟡 |
| P1 | 通知系统 | 3h | 🟡 |
| P2 | 用户体验优化 | 4h | 🟢 |
| P2 | 性能优化 | 3h | 🟢 |
| P2 | 管理功能增强 | 4h | 🟢 |

**总计: 38小时（约2周）**

---

## 七、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Stripe集成复杂 | 高 | 使用沙箱环境先行测试 |
| 数据迁移困难 | 中 | 先备份，后迁移 |
| 并发性能问题 | 中 | 添加Redis缓存 |

---

## 八、验收标准

### 功能验收

- [ ] 画廊展示真实用户生成的图片
- [ ] 点赞/收藏功能数据持久化
- [ ] 模板保存和加载正常
- [ ] 积分正确扣减和充值
- [ ] Stripe支付流程完成
- [ ] 通知实时推送

### 性能验收

- [ ] 画廊加载 < 2秒
- [ ] API响应 < 500ms
- [ ] 并发100用户无报错

---

*文档创建时间: 2026-04-25*  
*下次更新: 完成V3.0后*